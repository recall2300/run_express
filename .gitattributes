# WSL/Linux 에서 실행되는 파일은 항상 LF 로 체크아웃한다.
# (CRLF 가 섞이면 shebang 파싱 실패 / systemd 유닛 오류가 발생한다)
*.sh        text eol=lf
*.service   text eol=lf

# Windows 전용 스크립트는 CRLF 유지
*.bat       text eol=crlf
*.ps1       text eol=crlf
